import { useDispatch } from "react-redux";
import lessonContext from "../app/context/lessonContext";

async function TriggerNewProblem() {
  // const dispatch = useDispatch()
  // const answer = await lessonContext({
  //   lesson: lessonString,
  //   level: 1,
  //   problemNumber: problemNumber + 1,
  // });
  // dispatch(changeLessonProps(answer));
  return 2;
}

export default TriggerNewProblem;
